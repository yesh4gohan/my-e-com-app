import React, { Component } from 'react'
import {API} from "aws-amplify"
export default class Test extends Component {
    componentDidMount(){
        let myInit = {
           response: true,
       }
       API.get("resource name","/products/all",myInit)//three params resource name, endpoint, request body
       .then(res=>console.log(res))
       
    }
    create = () => {
        const load = {
            body:{
    "name": "dummy",
    "price": "1000",
    "cpu": "dummy",
    "camera": "dummy",
    "size": "dummy",
    "weight": "dummy",
    "battery": "dummy",
    "display": "dummy",
    "memory": "dummy"
            }
    };
           API.post("resource name", "/products",load)//three params resource name, endpoint, request body
           .then((res)=>console.log(res))
           .catch(err=>console.log(err))
            
    }
  render() {
    return (
      <div>
        <button onClick = {this.create}>create product</button>
      </div>
    )
  }
}

