import React, { Component } from "react";
import {Link} from 'react-router';
import { connect } from "react-redux";
import {getTotalPrice,getProductCount} from "selectors";
export class BasketCart extends Component {

  render() {
      
      const {totalPrice,totalProducts} = this.props;
    return (
      <div>
        <div className="cart">
          <div className="dropdown">
            <Link
              to="/basket"
              id="dLabel"
              className="btn btn-inverse btn-block btn-lg"
            >
              <i className="fa fa-fa-shopping-cart">
                <span>{`items ${totalProducts?totalProducts:0} Total ${totalPrice}`}</span>
              </i>
            </Link>
          </div>
        </div>
      </div>
    );
  }
}

const mapStateToProps = state => ({
  
  totalProducts:getProductCount(state),
  totalPrice:getTotalPrice(state)
});

const mapDispatchToProps = {};

export default connect(
  mapStateToProps,
  null
)(BasketCart);
