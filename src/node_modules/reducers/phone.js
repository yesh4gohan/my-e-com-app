import { FETCH_EACH_PHONES_SUCCESS } from "actionTypes";
const initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    case FETCH_EACH_PHONES_SUCCESS:
      return Object.assign({}, state,   action.payload );
    default:
      return state;
  }
};
